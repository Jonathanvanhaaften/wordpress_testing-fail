description: >
  # What will this job do?
  # Descriptions should be short, simple, and clear.
  wordpress testing and for self teaching
executor: default
parameters:
  greeting:
    type: string
    default: "Hello"
    description: "Hello welcome to wordpress"
steps:
  - greet:
      greeting: << parameters.greeting >>
